#include "thread_pool.hpp"
#include <cstdio>

bool stopped = false;
int wakeup_count = 0;

int main()
{
    printf("DoorLang");
    fflush(stdout);

    //exit(1);
    //assert(false);

    return 0;
}
